defaultNamespace: externaldns
helm:
  repo: oci://registry-1.docker.io/bitnamicharts
  chart: external-dns
  releaseName: externaldns
  valuesFiles:
  - values.yaml

targetCustomizations:
  - clusterGroup: default
    helm:
      version: 7.3.0

